//now let's add the color that we will need to project
import 'package:flutter/material.dart';

class MainColor {
  static Color primaryColor = const Color.fromARGB(136, 177, 177, 177);
  static Color secondaryColor = const Color.fromARGB(255, 62, 62, 62);
  static Color accentColor = const Color.fromARGB(255, 255, 255, 255);
}
